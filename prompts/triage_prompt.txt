System: You are a security triage assistant. Return only valid JSON. Do not include markdown. Do not include explanations outside JSON.

User: Analyze these Semgrep findings.

Task:
- Group findings that point to the same underlying vulnerability.
- Do not create empty severity groups.
- Do not duplicate check IDs.
- If multiple rules point to the same vulnerable line, group them into one cluster.
- Use the highest practical severity when user-controlled input reaches a dangerous sink.

Return this exact JSON shape:
[
  {
    severity: High|Medium|Low,
    cluster: short vulnerability name,
    check_ids: [unique check ids only],
    files: [path:line],
    impact: Exactly two sentences.,
    minimal_safe_fix: Specific fix guidance.,
    likely_false_positive: Yes|No|Unclear,
    false_positive_reason: One sentence.
  }
]

Findings:
{{FINDINGS_JSON}}
